# Настройка Яндекс.Метрики для бота "Духовник"

## 📊 Обзор

Бот интегрирован с Яндекс.Метрикой для отслеживания:
- Запусков бота и регистраций пользователей
- Активаций бесплатного периода
- Успешных платежей и конверсий

## 🔑 Получение OAuth токена для Management API

### Шаг 1: Создание приложения в Яндекс.OAuth

1. Перейдите на https://oauth.yandex.ru/
2. Нажмите "Зарегистрировать новое приложение"
3. Заполните форму:
   - **Название**: Духовник Бот Analytics
   - **Платформа**: Веб-сервисы
   - **Redirect URI**: https://oauth.yandex.ru/verification_code
4. В разделе **Доступы** выберите:
   - ✅ `metrika:read` - чтение данных счётчика
   - ✅ `metrika:write` - запись данных (для оффлайн-конверсий)
5. Нажмите "Создать приложение"
6. Скопируйте **Client ID**

### Шаг 2: Получение токена

1. Откройте в браузере URL (замените `YOUR_CLIENT_ID` на ваш Client ID):
   ```
   https://oauth.yandex.ru/authorize?response_type=token&client_id=YOUR_CLIENT_ID
   ```

2. Авторизуйтесь в Яндекс
3. Разрешите доступ приложению к Метрике
4. Вы будете перенаправлены на URL вида:
   ```
   https://oauth.yandex.ru/verification_code#access_token=YOUR_TOKEN&...
   ```
5. Скопируйте `access_token` - это и есть ваш OAuth токен

### Шаг 3: Добавление токена в .env

```bash
YANDEX_METRIKA_TOKEN=YOUR_ACCESS_TOKEN_HERE
```

## 🎯 Настройка целей в Яндекс.Метрике

Зайдите в https://metrika.yandex.ru/dashboard?id=106602878

### Настройки → Цели → Добавить цель

Создайте следующие цели:

| Название | ID цели | Тип | Условие |
|----------|---------|-----|---------|
| bot_start | 508977279 | JavaScript событие | Идентификатор: `bot_start` |
| free_period_start | 508976625 | JavaScript событие | Идентификатор: `free_period_start` |
| premium_payment | 508976841 | JavaScript событие | Идентификатор: `premium_payment` |

## 📈 Отслеживаемые конверсии

### 1. bot_start (Goal ID: 508977279)

**Триггер:** Первый запуск бота новым пользователем

**Когда отправляется:**
- Пользователь впервые запускает `/start`
- `user_data['onboarded']` = `False` (новый пользователь)

**Данные:**
- ClientID: Telegram user_id
- Value: 0 RUB (бесплатное событие)

**Код:**
```python
# handlers/start.py
if is_new_user:
    asyncio.create_task(send_offline_conversion_bot_start(user_id))
```

---

### 2. free_period_start (Goal ID: 508976625)

**Триггер:** Активация бесплатного периода (30 дней)

**Когда отправляется:**
- Пользователь нажимает кнопку "Активировать 1 месяц бесплатно"
- Callback: `activate_free_period`

**Данные:**
- ClientID: Telegram user_id
- Value: 0 RUB (бесплатное событие)

**Код:**
```python
# handlers/subscription.py → activate_free_period_handler
asyncio.create_task(send_offline_conversion_free_period(user_id))
```

---

### 3. premium_payment (Goal ID: 508976841)

**Триггер:** Успешная оплата Premium подписки

**Когда отправляется:**
- Пользователь успешно оплатил подписку (1, 3 или 12 месяцев)
- Telegram вызвал `successful_payment_handler`

**Данные:**
- ClientID: Telegram user_id
- Value: Сумма платежа в рублях (например, 149.00)
- Currency: RUB

**Код:**
```python
# handlers/subscription.py → successful_payment_handler
# payment_info.total_amount приходит в копейках
asyncio.create_task(send_offline_conversion_payment(user_id, payment_info.total_amount))
```

---

## 🔍 Проверка работы

### Логи бота

После выполнения действий проверьте логи:

```bash
tail -f /opt/dukhovnik_bot/bot.log | grep "Оффлайн-конверсия"
```

**Ожидаемые сообщения:**
```
2026-01-18 12:34:56 - Оффлайн-конверсия 508977279 отправлена для user_id=123456, price=0.0 RUB
2026-01-18 12:45:12 - Оффлайн-конверсия 508976625 отправлена для user_id=789012, price=0.0 RUB
2026-01-18 15:23:45 - Оффлайн-конверсия 508976841 отправлена для user_id=345678, price=149.0 RUB
```

### Яндекс.Метрика

1. Зайдите в https://metrika.yandex.ru/dashboard?id=106602878
2. Отчёты → Стандартные отчёты → Конверсии
3. Выберите период: последние 7 дней
4. Проверьте, что конверсии отображаются:
   - **bot_start** - количество новых пользователей
   - **free_period_start** - активации бесплатного периода
   - **premium_payment** - платежи с суммами

### API Testing

Вручную отправьте тестовую конверсию:

```bash
curl -X POST "https://api-metrika.yandex.net/management/v1/counter/106602878/offline_conversions/upload" \
  -H "Authorization: OAuth YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "conversions": [
      {
        "ClientID": "123456789",
        "Target": "508977279",
        "DateTime": 1705579200,
        "Price": 0.0,
        "Currency": "RUB"
      }
    ]
  }'
```

**Ожидаемый ответ (успех):**
```json
{
  "uploading": {
    "status": "ok",
    "comment": "Conversion uploaded"
  }
}
```

## 🐛 Troubleshooting

### Ошибка: "Invalid OAuth token"

**Проблема:** Токен устарел или неверный

**Решение:**
1. Проверьте, что токен скопирован полностью
2. Получите новый токен через OAuth (см. Шаг 2 выше)
3. Обновите `.env` файл
4. Перезапустите бота: `sudo systemctl restart dukhovnik-bot`

### Ошибка: "Goal not found"

**Проблема:** Цель с указанным ID не существует в счётчике

**Решение:**
1. Зайдите в Метрику → Настройки → Цели
2. Проверьте ID целей
3. Обновите константы `GOAL_*` в `core/yandex_metrika.py`

### Конверсии не отображаются в Метрике

**Причины:**
1. **Задержка обработки** - оффлайн-конверсии могут появиться в отчётах через 1-2 часа
2. **Неверный ClientID** - проверьте, что используется `str(user_id)`
3. **Токен без прав `metrika:write`** - пересоздайте токен с правильными доступами

## 📊 Анализ данных

### Воронка конверсии

```
bot_start (все новые пользователи)
  ↓
free_period_start (активировали бесплатный доступ)
  ↓
premium_payment (оплатили подписку)
```

**Метрики для отслеживания:**
- **Конверсия start → free_period**: сколько новых пользователей активируют бесплатный период
- **Конверсия free_period → payment**: сколько пользователей переходят в платную подписку
- **LTV (Lifetime Value)**: средняя сумма платежей на пользователя

### Рекомендуемые отчёты

1. **Конверсия по источникам трафика**
   - Группировка по `utm_source`
   - Какой источник приносит платящих пользователей

2. **Динамика платежей**
   - Сумма платежей по дням/неделям/месяцам
   - Goal: `premium_payment` с параметром Price

3. **Эффективность бесплатного периода**
   - Процент пользователей, оплативших после бесплатного периода

## 🔒 Безопасность

- ✅ `.env` файл **не коммитится** в git (в `.gitignore`)
- ✅ Токен хранится локально на сервере
- ✅ Все запросы к API логируются
- ✅ Таймауты предотвращают зависание при сбоях API

## 📝 Примечания

- Оффлайн-конверсии отправляются асинхронно (`asyncio.create_task`)
- Сбои API не влияют на работу бота
- ClientID = Telegram user_id (стабильный идентификатор)
- Конверсии обрабатываются в Метрике с задержкой до 2 часов
